<div class="p-5 mb-4 bg-light rounded-3">
  <div class="container-fluid py-5">
  <h1 class = "fw-bold"><%= @pet.name.capitalize %></h1>

  <h6 class = "text-muted">Type: <%= @pet.species.capitalize %></h3>

  <p class = "fw-light text-muted">Found on <%= @pet.found_on %> near <%= @pet.address %></p>

  <%# Remember with link_to, the string is the text of the link, and the second argument should always be the path to wherever you want the link to go, then you can add other elements such as classes, etc %>
  <%= link_to 'Go back home', pets_path, class: "btn btn-primary btn-lg"%>
  </div>
</div>
